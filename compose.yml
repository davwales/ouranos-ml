services:
  ouranos-ml:
    image: ghcr.io/davwales/ouranos-ml:latest
    ports:
      - "8000:8000"
    environment:
      LLM_BASE_URL: host.docker.internal:11434
      LLM_OPENAI_BASE_URL: http://host.docker.internal:11434/v1
      LLM_MODEL_TTL: 300
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
